# Design System

Use the design system from the /reference folder for all UI decisions.

Read /reference/design-system.md before writing any UI code.

Apply the design system to all components including colors, typography,
layout dimensions, spacing, and component styles.